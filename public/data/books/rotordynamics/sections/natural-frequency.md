# 危険速度はどこで決まる？

前の節で、回転数が「固有振動数」に一致すると揺れが急に大きくなる——危険速度——を見ました。では、その固有振動数はそもそも**何で決まる**のでしょう。

答えはとてもシンプルで、効くのは2つだけです。**軸の硬さ（ばねの強さ）$k$** と **円板の重さ $m$**。固有振動数 $\omega_n$ は、この2つの比の平方根で決まります。

$$\omega_n = \sqrt{\frac{k}{m}}$$

つまり、**軸を硬くすると $\omega_n$ は上がり**、**円板を重くすると $\omega_n$ は下がる**。下のグラフで $k$ と $m$ を動かして、危険速度の山が左右に動くのを確かめてください。前の節では山の位置は動きませんでしたが、今度は**山そのものが移動**します。

:::function-plot
{
  "title": "k と m で、危険速度の山が左右に動く",
  "xKey": "Omega",
  "xLabel": "回転の速さ  Ω (rad/s)",
  "yLabel": "揺れの大きさ  R / e",
  "xMin": 0,
  "xMax": 1500,
  "yMin": 0,
  "yMax": 6,
  "expr": "(Omega/sqrt(k*1e6/m))^2 / sqrt((1 - (Omega/sqrt(k*1e6/m))^2)^2 + (2*0.1*(Omega/sqrt(k*1e6/m)))^2)",
  "sliders": [
    { "key": "k", "label": "軸の硬さ k（MN/m）", "min": 2, "max": 20, "step": 0.5, "default": 10 },
    { "key": "m", "label": "円板の重さ m（kg）", "min": 2, "max": 40, "step": 1, "default": 10 }
  ],
  "caption": "山の位置 ＝ 固有振動数 ωn = √(k/m)。硬く（k大）すると右へ、重く（m大）すると左へ動きます。"
}
:::

ここから、設計の勘どころが見えてきます。回転機械を**ある回転数で使いたい**なら、その回転数が危険速度（山）に重ならないように、軸の硬さや円板の重さを選んで ωn をずらしてやればよい——これが「危険速度を避ける設計」の出発点です。

**模式・出典について**：上の図は前の節と同じジェフコットロータの不釣り合い応答に、ωn = √(k/m) を代入して回転速度 Ω を横軸にしたものです（減衰比は ζ = 0.1 に固定）。式そのままで、値に脚色はありません。
